#### Local session manager with express-session and passport

Signup, login, logout and protected routes are supported.

Example of use:

In the terminal run:

    $ yarn install
    $ yarn start

To test it use postman or similar to emulate a complete request
